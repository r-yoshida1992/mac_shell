#!/bin/zsh
# List all files and directories, including hidden ones, in long format
# The -a option shows hidden files
# The -l option displays the files in long format, including permissions, owner, size, and modification time
# The -F option appends a symbol to each entry indicating its type (e.g., / for directories, * for executables)
ls -laF
